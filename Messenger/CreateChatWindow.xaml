<Window x:Class="WPF_Messenger.CreateChatWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Create Chat" Height="200" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Введите ваше имя:" Margin="5"/>
        <TextBox x:Name="UsernameBox" Grid.Row="1" Margin="5"/>

        <Button Content="Создать чат" Grid.Row="2" Click="CreateChatButton_Click" Margin="5"/>
    </Grid>
</Window>